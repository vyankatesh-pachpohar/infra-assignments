apiVersion: v1
kind: Service
metadata:
  name: config-service-postgres
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: config-service-postgres
  ports:
    - port: 5432
      targetPort: 5432
