apiVersion: v1
kind: Service
metadata:
  name: config-service-app
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: config-service-app
  ports:
    - port: 8080
      targetPort: {{ .Values.app.port }}
      nodePort: 30080
